Skip to content
Search or jump to…
Pull requests
Issues
Codespaces
Marketplace
Explore
 
@jonot-cyber 
jonot-cyber
/
sitelen-pona-tester
Private
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
sitelen-pona-tester/.github/workflows/static.yml
@jonot-cyber
jonot-cyber Take two
Latest commit e9cbab7 9 minutes ago
 History
 1 contributor
45 lines (40 sloc)  1.11 KB

# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Setup Node.js environment
        uses: actions/setup-node@v3.5.1
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node.js environment
        uses: actions/setup-node@v3.5.1
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install Dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Deploy
        uses: actions/upload-pages-artifact@v1.0.7
        with:
          path: build
Footer
© 2023 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Docs
Contact GitHub
Pricing
API
Training
Blog
About
sitelen-pona-tester/static.yml at main · jonot-cyber/sitelen-pona-tester
